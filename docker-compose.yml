version: '3.8'

services:
  oracle-ai:
    build: .
    ports:
      - "5173:5173"  # Frontend
      - "8080:8080"  # GraphQL
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    volumes:
      - ./:/app
    command: /app/run.sh
